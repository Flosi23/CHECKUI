export const Story = ({title, id}) => {
    return (<>
        <h3>{title}</h3>
        <Frame>
            <iframe
                src={`http://localhost:3002/iframe.html?viewMode=story&id=${id}`}
                title="Button" width="100%"
                height="200px"></iframe>
        </Frame>
    </>);
}